import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    rootContainer: {
        borderWidth: 2,
        borderColor: 'black',
        width: '95%'
    }
})

export default styles;